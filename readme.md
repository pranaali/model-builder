# Modal Builder

Create data modals dynamically by using Modal Builder, integratable with MondoDB

## APIs

```
createModel();

saveFields();

addField();

getDynamicModel();

getModelFields();

removeField();
```
